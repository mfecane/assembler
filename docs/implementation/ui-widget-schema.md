# Root configuration controls

Root graph configuration lives in the root-graph record, outside the graph node model. Exported input
nodes are ordinary raw inputs in every graph, including root graphs. Exporting a supported primitive input
adds its default control to the root configuration layer.
