# User menu

The authenticated user menu uses the shadcn Avatar primitives for the profile image. It displays
the Supabase `avatar_url` when available and falls back to initials derived from the user's name or
email. The email and sign-out action remain visible alongside the avatar.
