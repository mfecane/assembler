Model context protocol is needed to be able to edit graphs using LLM's.
We can export project graph as JSON. We also need metadata JSON with, for example, asset sizes and offsets.
What else do we need to develop robust, industry standard, production level MCP?
